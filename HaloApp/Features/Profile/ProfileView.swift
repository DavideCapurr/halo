import SwiftUI
import HaloShared

/// Step 5 / 9: profilo proprio e altrui, con vibe + halo space filtrato per tier.
struct ProfileView: View {
  let userId: UUID
  var body: some View {
    Text("Profile \(userId) — TODO step 5/9").foregroundStyle(.white)
  }
}
