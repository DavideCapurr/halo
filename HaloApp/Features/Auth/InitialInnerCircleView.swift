import SwiftUI

/// Step 4: selezione/invito dei primi 3-5 Inner durante onboarding.
/// Critico per cold start (atomic network).
struct InitialInnerCircleView: View {
  var body: some View {
    Text("Scegli i primi Inner — TODO step 4").foregroundStyle(.white)
  }
}
