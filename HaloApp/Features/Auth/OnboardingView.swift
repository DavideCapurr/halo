import SwiftUI

/// Step 4: onboarding — handle, display name, avatar, vibe iniziale.
struct OnboardingView: View {
  var body: some View {
    Text("Onboarding — TODO step 4").foregroundStyle(.white)
  }
}
