import SwiftUI
import AVFoundation

/// Step 6: registratore audio max 30s via AVAudioRecorder.
struct AudioRecorderView: View {
  var body: some View {
    Text("Audio recorder — TODO step 6").foregroundStyle(.white)
  }
}
