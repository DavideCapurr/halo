import SwiftUI
import HaloShared

/// Step 9: halo space = galleria verticale post non scaduti di un utente.
struct HaloSpaceView: View {
  let userId: UUID
  var body: some View {
    Text("Halo Space di \(userId) — TODO step 9").foregroundStyle(.white)
  }
}
